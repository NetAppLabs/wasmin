#!/bin/bash

VERSION="latest"

MOUNT_CURDIR=true
DOCKER_FLAGS=""

if [ "${MOUNT_CURDIR}" == "true" ]; then
		LOCAL_MOUNTDIR="$(pwd)"
		DOCKER_FLAGS="--mount type=bind,source=${LOCAL_MOUNTDIR},target=/mount"
fi
		
docker run -it ${DOCKER_FLAGS} wasmin:${VERSION} $*
