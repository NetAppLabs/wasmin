{
  "name": "@wasm-env/node-shell",
  "version": "0.0.0",
  "scripts": {
    "build": "vite build --sourcemap && yarn tsc",
    "start": "node --enable-source-maps dist/node-shell.umd.js",
    "build-esbuild": "esbuild --bundle src/index.ts --sourcemap --platform=node --outfile=dist/out.js --main-fields=main,module",
    "start-esbuild": "node --enable-source-maps dist/out.js",
    "clean": "rm -rf node_modules",
    "run": "node dist/out.js"
  },
  "devDependencies": {
    "esbuild": "^0.15.12",
    "typescript": "^4.8.4",
    "vite": "^3.1.8"
  },
  "dependencies": {
    "@wasm-env/fs-js": "*",
    "@wasm-env/node-fs-js": "*",
    "@wasm-env/wasi-js": "*",
    "node-fetch": "^3.2.10",
    "web-file-polyfill": "^1.0.4",
    "web-streams-polyfill": "^3.2.1"
  }
}
